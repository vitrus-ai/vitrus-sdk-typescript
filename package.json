{
  "name": "@vitrus/sdk-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "One SDK, any Droid: a unified interface for robots running VitrusOS across TypeScript, Python, Web and LAN mesh.",
  "scripts": {
    "build:typescript": "cd typescript && bun run build",
    "test:typescript": "cd typescript && bun test",
    "check:typescript": "cd typescript && bun run build && bun test",
    "test:python": "PYTHONPATH=python python3 -m pytest python/tests -q",
    "check:docs": "test -f docs/index.html && test -f docs/architecture.md",
    "check": "bun run check:typescript && bun run test:python && bun run check:docs",
    "docs:serve": "python3 -m http.server 4173 --directory docs"
  }
}
